{
  "name": "mentoria-devops-pipeline",
  "version": "1.0.0",
  "description": "Demo site para pipeline CI/CD com GitHub Actions",
  "main": "dist/server.js",
  "scripts": {
    "build": "node build.js",
    "start": "node dist/server.js",
    "dev": "node src/server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
